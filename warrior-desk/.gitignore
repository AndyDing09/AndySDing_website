# Secrets and the live lock — NEVER commit these.
.env
live_account_acknowledgement.md
credentials.json
token.json

# Runtime artifacts (anchored to the project root so they don't match the
# warrior/journal/ source package).
/state/
/logs/
/journal/
/config.yaml

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
build/
dist/
.venv/
venv/
